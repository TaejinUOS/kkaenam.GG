# 깨남.GG 프로젝트 지침

## 제품 및 디자인 기준

- 기능 범위와 사용자 흐름의 기준 문서는 `PRD.md`다.
- UI를 구현하거나 수정하기 전에 `DESIGN_BLUEPRINT.md`를 읽는다.
- `DESIGN_BLUEPRINT.md`와 아래 생성 목업을 깨남.GG의 승인된 시각 기준으로 취급한다.
  - `design/blueprints/category-selection-v1.png`
  - `design/blueprints/ahri-matchup-v1.png`
- 핵심 방향은 **GLOWING MATCHUP ZINE — 야광 상대법 잡지**다. 비대칭 에디토리얼 그리드, STATIC BLOOM 팔레트, 강한 한글 타이포그래피, 포스터·스티커·형광펜 메모 문법을 일관되게 확장한다.
- 상대법 탐색은 `탑 / 정글 / 미드 / 원딜 / 서폿` 포지션과 포지션별 카테고리의 2단계 구조를 사용하며, 카테고리명과 커버 이미지는 `PRD.md` 5.1의 매핑을 기준으로 한다.
- `전적`, `통계`, `티어표`는 MVP에서 메뉴와 `추후 개발` 안내 화면만 제공한다. 실제 API 호출, 가짜 통계와 임시 티어 결과를 구현하지 않는다.
- 챔피언 상대법 페이지의 Aside 비주얼은 목업 구도를 따른 정적 2D 일러스트로 통일한다. 3D 모델, WebGL, 자동 회전과 드래그 회전은 구현하지 않는다.
- 새 화면은 같은 잡지의 다음 페이지처럼 설계한다. 범용 SaaS 대시보드, 반복되는 둥근 카드, glassmorphism, 보라색·파란색 mesh gradient로 회귀하지 않는다.
- 목업은 픽셀 단위 명세나 완성 콘텐츠가 아니라 구도, 정보 위계, 질감과 인상의 기준이다. 이미지 속 임시 문구·캐릭터·플레이스홀더를 그대로 제품 데이터로 사용하지 않는다.
- 반응형 사용성, 접근성, 성능, 실제 데이터 정확성, 에셋 사용 권리가 목업 재현보다 우선한다. 필요한 조정에서도 시각적 의도를 최대한 유지하고, 방향을 바꾸는 결정은 `DESIGN_BLUEPRINT.md`에 기록한다.

## 전문 Sub-agent 운영

- 프로젝트 전용 에이전트 정의는 `.codex/agents/`에 둔다.
- 사용자가 특정 역할을 지정하거나 전문 영역별 병렬 작업을 요청하면 아래 에이전트에 서로 겹치지 않는 범위를 위임한다.
  - `product-manager`: PRD, 제품 범위, 사용자 요구사항, 일정, 우선순위와 인수 조건
  - `backend-developer`: 서버 아키텍처, API, 데이터, 외부 연동, 보안과 서버 성능
  - `frontend-developer`: UI 구현, 반응형 디자인, 접근성, 브라우저 성능
  - `qa-engineer`: 기능 검증, 오류·회귀·성능 점검, 코드 리뷰와 개선 제안
  - `ai-integration-specialist`: LLM 연동, 프롬프트·평가, 파인튜닝 판단과 AI 파이프라인
- 제품 범위나 인수 조건이 불명확하면 `product-manager`가 먼저 기준을 정리한다.
- 백엔드와 프론트엔드는 API 계약을 먼저 합의한 뒤 독립적으로 작업하고, 동일 파일을 동시에 수정하지 않는다.
- 구현이 끝나면 `qa-engineer`가 독립적으로 검증하며, 메인 에이전트가 모든 결과와 미해결 위험을 종합한다.
