/**
 * "분류 없음" 통을 가리키는 예약 키. `/wiki?분류=` 질의에서 쓴다.
 *
 * 이 상수만 따로 파일을 둔 것은 순전히 번들 크기 때문이다. `src/data/wikiIndex.ts`는
 * `buildWikiIndexData`가 챔피언 카탈로그(스킬 설명까지 포함해 수십 KB)를 끌어오는
 * `wikiLink.ts`에 값으로 의존하는 무거운 모듈이라, 서버 컴포넌트에서만 값으로
 * import해야 한다. `WikiIndexScreen.tsx`(클라이언트 컴포넌트)는 `?분류=` 질의를 이
 * 값과 비교해야 하는데, `wikiIndex.ts`에서 이 상수 하나만 가져오려 해도 그 모듈
 * 전체(그리고 챔피언 카탈로그까지)가 클라이언트 번들에 실린다 — 실제로 한 번
 * 그렇게 만들었다가 `/wiki` 번들이 4kB에서 70kB로 뛴 것을 보고 여기로 뺐다.
 *
 * 실제 관문 이름과 겹치지 않게 대괄호로 감싼다. `checkArticleTitle`이 대괄호가 든
 * 이름을 애초에 막으므로, 진짜 분류 이름이 이 값과 부딪힐 일은 없다.
 */
export const UNCATEGORIZED_KEY = "[없음]";
